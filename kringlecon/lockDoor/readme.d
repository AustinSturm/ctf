## De Bruijn sequence

de Bruijn sequence generates a string of aall possible permutations entered to reduce the full brure force method.

I just generated all combinations as it is just 4^4

Otherwise a linux one-liner for sequence generation into curl from a solved string would do.
